# Hosted Postgres (Supabase or Neon pooled URL). Never prefix this with NEXT_PUBLIC.
DATABASE_URL=
# Local development only; never used on Vercel. Defaults to .local-db.
LOCAL_DATABASE_PATH=.local-db
# Optional authenticated timeout maintenance endpoint. Use at least 32 random bytes.
CRON_SECRET=
