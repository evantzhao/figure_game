'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';
import { Trophy, ArrowUpRight } from 'lucide-react';
import { api, useApp, type User } from '@/components/providers';
export default function Leaderboard(){const{online}=useApp();const[players,setPlayers]=useState<User[]>([]),[error,setError]=useState('');useEffect(()=>{if(online)void api<{players:User[]}>('leaderboard').then(data=>setPlayers(data.players)).catch(e=>setError(e.message));},[online]);return <div className="content-page"><p className="eyebrow">THE CLUB LADDER</p><h1>A rating earned on the board.</h1><p className="muted">Elo begins at 1500. Only verified rated human games can change it; practice and casual games never do.</p>{error&&<div className="message error">{error}</div>}{players.length?<table className="rating-table"><thead><tr><th>Rank</th><th>Player</th><th>Elo</th><th>Rated games</th></tr></thead><tbody>{players.map((p,i)=><tr key={p.id}><td>{i+1}</td><td>{p.username}</td><td>{p.rating}</td><td>{p.rated_games}</td></tr>)}</tbody></table>:<section className="card empty-state"><Trophy size={32}/><h2 style={{marginTop:18}}>Good competition starts with fair rules.</h2><p>Rated play is locked while the complete Xiangqi repetition and chase rules are verified. You can already practice, invite a friend, or join the casual queue. No placeholder players or fabricated rankings.</p><Link href="/" className="button primary">Play a casual game <ArrowUpRight size={17}/></Link></section>}</div>}
